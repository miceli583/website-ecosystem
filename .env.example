# Since the ".env" file is gitignored, you can use the ".env.example" file to
# build a new ".env" file when you clone the repo. Keep this file up-to-date
# when you add new variables to `.env`.

# This file will be committed to version control, so make sure not to have any
# secrets in it. If you are cloning this repo, create a copy of this file named
# ".env" and populate it with your secrets.

# When adding additional environment variables, the schema in "/src/env.js"
# should be updated accordingly.

# Supabase Authentication (Required)
NEXT_PUBLIC_SUPABASE_URL="https://wuxmtvdfzpjonzupmgsd.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Ind1eG10dmRmenBqb256dXBtZ3NkIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NTczNjM2NjIsImV4cCI6MjA3MjkzOTY2Mn0.uH4bWLJhyx3BJyc2Gz3MRhbA0b2hrOPWboIhAZJrJBc"

# Supabase Service Role Key (Required for server-side operations like Storage uploads)
# Get from: Supabase Dashboard → Project Settings → API → service_role key (secret)
# WARNING: This key bypasses RLS - keep it secret, never expose to client
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key-here"

# Database Configuration
# For development (SQLite/LibSQL):
DATABASE_URL="file:./db.sqlite"

# For production (PostgreSQL/Supabase):
# DATABASE_URL="postgresql://postgres.wuxmtvdfzpjonzupmgsd:[YOUR-PASSWORD]@aws-1-us-east-2.pooler.supabase.com:6543/postgres"

# Bearer auth secret for cron/automation endpoints
# Generate with: openssl rand -hex 32
CRON_SECRET="your-cron-secret-here"

# Make.com Webhook for Instagram Posting
# Get this from Make.com: Create a new scenario → Add Webhook module → Copy webhook URL
NEXT_PUBLIC_MAKE_WEBHOOK_URL="https://hook.us2.make.com/your-webhook-id-here"
NEXT_PUBLIC_MAKE_API_KEY="your-api-key-here"

# Stripe Configuration
# Test keys for development (portal checkout, subscriptions)
STRIPE_SECRET_KEY="sk_test_your-test-key-here"
STRIPE_WEBHOOK_SECRET="whsec_your-webhook-secret-here"

# Stripe Live Read-Only Key (for admin finance dashboard)
# Create a restricted key at: https://dashboard.stripe.com/apikeys/create
# Enable only: customers.read, subscriptions.read, invoices.read, charges.read, balance.read
STRIPE_LIVE_READ_KEY="rk_live_your-restricted-key-here"

# Mercury API (for bank account data in finance dashboard)
# Get from: https://mercury.com/settings/tokens
MERCURY_API_KEY="secret-token:your-mercury-api-key-here"
